# Sunny Test Live React

This repository hosts a simple **React HR dashboard** built with Vite. It showcases leadership metrics, department health, and team focus spots using a clean card-based layout.

## Scripts

- `npm install`: install dependencies
- `npm run dev`: run the local Vite dev server
- `npm run build`: build the production bundle
- `npm run preview`: preview the production build locally
